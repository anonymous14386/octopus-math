services:
  octopus-math:
    build: .
    container_name: octopus-math
    restart: unless-stopped
    ports:
      - "3011:3011"
    volumes:
      - math_data:/app/data
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - MATH_MODEL=${MATH_MODEL:-claude-sonnet-4-6}
      - AUTH_SERVICE_URL=${AUTH_SERVICE_URL:-http://octopus-auth:3002}
      - SESSION_SECRET=${SESSION_SECRET}
      - PORT=3011
      - NODE_ENV=production
    networks:
      - octopus_network

volumes:
  math_data:

networks:
  octopus_network:
    external: true
